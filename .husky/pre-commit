#!/bin/sh

echo "Running validation (typecheck + tests)..."
npm run validate
if [ $? -ne 0 ]; then
  echo "Validation failed. Please fix errors before committing."
  exit 1
fi

echo "Pre-commit checks passed."
